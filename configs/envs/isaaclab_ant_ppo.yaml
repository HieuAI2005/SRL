# PPO on Isaac Lab — Isaac-Ant-v0  (obs=60, act=8)
# Requires IsaacLab + IsaacSim
# Target return: ~5000 (convergence ~5M steps)

env_id: "Isaac-Ant-v0"
env_type: "isaaclab"

algo: ppo

encoders:
  - name: actor_state_enc
    input_name: state
    type: mlp
    input_dim: 60
    latent_dim: 256
    layers:
      - {out_features: 256, activation: elu, norm: none}
      - {out_features: 256, activation: elu, norm: none}
      - {out_features: 128, activation: elu, norm: none}
  - name: critic_state_enc
    input_name: state
    type: mlp
    input_dim: 60
    latent_dim: 256
    layers:
      - {out_features: 256, activation: elu, norm: none}
      - {out_features: 256, activation: elu, norm: none}
      - {out_features: 128, activation: elu, norm: none}

flows:
  - "actor_state_enc -> actor"
  - "critic_state_enc -> critic"

actor:
  name: actor
  type: gaussian
  action_dim: 8
  log_std_init: -1.0
  layers:
    - {out_features: 128, activation: elu, norm: none}

critic:
  name: critic
  type: value
  layers:
    - {out_features: 128, activation: elu, norm: none}

losses:
  - {name: policy,  weight: 1.0}
  - {name: value,   weight: 1.0}
  - {name: entropy, weight: 0.005}

train:
  total_steps: 5_000_000
  n_envs: 4096
  n_steps: 32
  batch_size: 16384
  n_epochs: 5
  lr: 5e-4
  gamma: 0.99
  gae_lambda: 0.95
  clip_range: 0.2
  entropy_coef: 0.005
  vf_coef: 1.0
  max_grad_norm: 1.0
